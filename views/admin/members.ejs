<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Members - Alumni System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = { theme: { extend: { colors: { primary: '#1e3a8a', secondary: '#1e40af' } } } }
    </script>
    <style>
        .glass {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .bg-pattern {
            background-image: radial-gradient(#1e3a8a 0.5px, transparent 0.5px);
            background-size: 20px 20px;
        }
    </style>
</head>

<body class="bg-gray-50 text-gray-900 bg-pattern min-h-screen">
    <div class="flex h-screen overflow-hidden relative">
        <!-- Sidebar Overlay (Mobile) -->
        <div id="sidebarOverlay" onclick="toggleSidebar()"
            class="fixed inset-0 bg-black/50 z-[55] hidden md:hidden transition-opacity"></div>

        <!-- Sidebar -->
        <aside id="adminSidebar"
            class="fixed md:static inset-y-0 left-0 w-64 glass bg-primary/95 text-white flex flex-col border-r border-white/10 z-[60] transform -translate-x-full md:translate-x-0 transition-transform duration-300 ease-in-out">
            <div class="p-8">
                <h2 class="text-xl font-black tracking-tight">ADMIN PANEL</h2>
                <div class="h-1 w-12 bg-white/20 mt-2 rounded-full"></div>
            </div>
            <nav class="flex-1 px-4 space-y-2">
                <a href="/admin/dashboard"
                    class="flex items-center gap-3 py-3 px-6 rounded-2xl bg-white/10 text-white font-bold transition">
                    <span>👥</span> Members
                </a>
                <a href="/admin/payments"
                    class="flex items-center gap-3 py-3 px-6 rounded-2xl hover:bg-white/5 text-white/70 font-bold transition">
                    <span>💰</span> Payments
                </a>
                <a href="/admin/welfare"
                    class="flex items-center gap-3 py-3 px-6 rounded-2xl hover:bg-white/5 text-white/70 font-bold transition">
                    <span>🏥</span> Welfare
                </a>
                <a href="/admin/messages"
                    class="flex items-center gap-3 py-3 px-6 rounded-2xl hover:bg-white/5 text-white/70 font-bold transition">
                    <span>💬</span> Messages
                </a>
            </nav>
            <div class="p-4">
                <button onclick="handleLogout()"
                    class="w-full flex items-center justify-center gap-3 py-3 rounded-2xl bg-red-500/10 text-red-400 hover:bg-red-500/20 font-bold transition">
                    <span>🚪</span> Logout
                </button>
            </div>
        </aside>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden">
            <header
                class="glass bg-white/70 sticky top-0 z-50 p-4 px-4 md:px-8 flex justify-between items-center border-b border-gray-200">
                <div class="flex items-center gap-3 md:gap-4">
                    <!-- Mobile Hamburger -->
                    <button onclick="toggleSidebar()" class="md:hidden p-2 hover:bg-gray-100 rounded-lg">
                        <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 6h16M4 12h16M4 18h16"></path>
                        </svg>
                    </button>

                    <h2 class="text-lg md:text-xl font-semibold text-gray-800 truncate">Members Management</h2>
                    <div class="hidden lg:block border-l pl-4 border-gray-200">
                        <p class="text-[10px] uppercase tracking-widest text-gray-400 font-bold">Developer</p>
                        <p class="text-xs font-semibold text-gray-600">Engr Usman Umar</p>
                    </div>
                </div>
                <div class="flex items-center space-x-2 md:space-x-4">
                    <button class="bg-primary text-white p-2 md:px-4 md:py-2 rounded-lg text-sm hover:bg-blue-800">
                        <span class="hidden md:inline">Add Member</span>
                        <span class="md:hidden">➕</span>
                    </button>
                    <button
                        class="border border-gray-300 text-gray-700 p-2 md:px-4 md:py-2 rounded-lg text-sm hover:bg-gray-50">
                        <span class="hidden md:inline">Export CSV</span>
                        <span class="md:hidden">📥</span>
                    </button>
                    <button onclick="handleLogout()"
                        class="bg-red-500 hover:bg-red-600 text-white p-2 md:px-4 md:py-2 rounded-lg text-sm font-bold transition">
                        <span class="hidden md:inline">Logout</span>
                        <span class="md:hidden">🚪</span>
                    </button>
                </div>
            </header>

            <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-100 p-6">
                <!-- Stats Row -->
                <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-6">
                    <div class="bg-white rounded-xl shadow p-5 border-l-4 border-blue-500">
                        <div class="flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500 font-medium">Total Members</p>
                                <h3 class="text-2xl font-bold text-gray-800" id="totalMembers">0</h3>
                            </div>
                            <div class="bg-blue-100 p-3 rounded-full text-blue-600 text-xl font-bold">👥</div>
                        </div>
                    </div>

                    <div class="bg-white rounded-xl shadow p-5 border-l-4 border-green-500">
                        <div class="flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500 font-medium">Total Raised (Annual)</p>
                                <h3 class="text-2xl font-bold text-gray-800" id="annualTotal">₦0</h3>
                            </div>
                            <div class="bg-green-100 p-3 rounded-full text-green-600 text-xl font-bold">💰</div>
                        </div>
                    </div>

                    <div class="bg-white rounded-xl shadow p-5 border-l-4 border-purple-500">
                        <div class="flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500 font-medium">Collected (This Month)</p>
                                <h3 class="text-2xl font-bold text-gray-800" id="monthlyCollected">₦0</h3>
                            </div>
                            <div class="bg-purple-100 p-3 rounded-full text-purple-600 text-xl font-bold">📈</div>
                        </div>
                    </div>

                    <div class="bg-white rounded-xl shadow p-5 border-l-4 border-orange-500">
                        <div class="flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500 font-medium">Pending (This Month)</p>
                                <h3 class="text-2xl font-bold text-gray-800" id="monthlyPending">₦0</h3>
                            </div>
                            <div class="bg-orange-100 p-3 rounded-full text-orange-600 text-xl font-bold">⏳</div>
                        </div>
                    </div>
                </div>

                <div
                    class="bg-blue-50 border border-blue-100 p-4 rounded-xl mb-6 flex flex-col md:flex-row items-center justify-between gap-4">
                    <div class="text-center md:text-left">
                        <h4 class="text-blue-800 font-bold">System Status: Active</h4>
                        <p class="text-blue-600 text-sm">Monthly contribution set to ₦1,500 per member.</p>
                    </div>
                    <div class="flex flex-col sm:flex-row gap-2 w-full md:w-auto">
                        <button onclick="triggerMonthly()"
                            class="bg-primary text-white px-4 py-3 md:py-2 rounded-lg text-sm hover:bg-blue-800 shadow-md transform active:scale-95 transition font-bold w-full sm:w-auto">
                            🔄 Generate Monthly Dues
                        </button>
                        <button onclick="sendReminders()"
                            class="bg-orange-600 text-white px-4 py-3 md:py-2 rounded-lg text-sm hover:bg-orange-700 shadow-md transform active:scale-95 transition font-bold w-full sm:w-auto">
                            🔔 Send Reminders
                        </button>
                    </div>
                </div>

                <!-- Members Table -->
                <div class="bg-white rounded-xl shadow">
                    <div class="px-6 py-4 border-b border-gray-100">
                        <h3 class="text-lg font-bold text-gray-800">Registered Alumni</h3>
                    </div>
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Name
                                    </th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Email
                                    </th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Role
                                    </th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Score
                                    </th>
                                    <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">Actions
                                    </th>
                                </tr>
                            </thead>
                            <tbody id="membersTable" class="bg-white divide-y divide-gray-200">
                                <tr>
                                    <td colspan="5" class="px-6 py-4 text-center text-gray-500">Loading...</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('adminSidebar');
            const overlay = document.getElementById('sidebarOverlay');
            if (sidebar.classList.contains('-translate-x-full')) {
                sidebar.classList.remove('-translate-x-full');
                overlay.classList.remove('hidden');
            } else {
                sidebar.classList.add('-translate-x-full');
                overlay.classList.add('hidden');
            }
        }

        async function handleLogout() { localStorage.removeItem('access_token'); window.location.href = '/login'; }

        async function fetchMembers() {
            const token = localStorage.getItem('access_token');
            try {
                const res = await fetch('/api/v1/admin/members', {
                    headers: { 'Authorization': `Bearer ${token}` }
                });
                if (res.ok) {
                    const members = await res.json();
                    document.getElementById('totalMembers').textContent = members.length;

                    const tbody = document.getElementById('membersTable');
                    tbody.innerHTML = members.map(m => `
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">${m.full_name}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                ${m.email}
                                <br>
                                <span class="text-[10px] ${m.is_active ? 'text-green-500' : 'text-red-500'} font-bold uppercase tracking-widest">
                                    ${m.is_active ? '● Active' : '○ Deactivated'}
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="px-3 py-1 text-[10px] font-black uppercase tracking-widest rounded-lg bg-blue-100 text-blue-800">${m.role}</span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 font-bold">${m.contribution_score || 0}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium space-x-3">
                                <button onclick="toggleStatus('${m._id}', ${m.is_active})" 
                                    class="${m.is_active ? 'text-red-600' : 'text-green-600'} font-bold hover:underline">
                                    ${m.is_active ? 'Deactivate' : 'Activate'}
                                </button>
                                <button class="text-primary font-bold hover:underline">Edit</button>
                            </td>
                        </tr>
                    `).join('');
                }
            } catch (e) { console.error(e); }
        }

        async function toggleStatus(id, currentStatus) {
            const newStatus = !currentStatus;
            if (!confirm(`Are you sure you want to ${newStatus ? 'activate' : 'deactivate'} this member?`)) return;

            const token = localStorage.getItem('access_token');
            try {
                const res = await fetch(`/api/v1/admin/members/${id}/status`, {
                    method: 'PATCH',
                    headers: {
                        'Content-Type': 'application/json',
                        'Authorization': `Bearer ${token}`
                    },
                    body: JSON.stringify({ is_active: newStatus })
                });

                if (res.ok) {
                    fetchMembers();
                } else {
                    alert("Status update failed.");
                }
            } catch (e) { console.error(e); }
        }

        async function fetchStats() {
            const token = localStorage.getItem('access_token');
            try {
                // Monthly Summary
                const resMonthly = await fetch('/api/v1/reports/summary/monthly', { headers: { 'Authorization': `Bearer ${token}` } });
                if (resMonthly.ok) {
                    const data = await resMonthly.json();
                    document.getElementById('monthlyCollected').textContent = `₦${data.collected.toLocaleString()}`;
                    document.getElementById('monthlyPending').textContent = `₦${data.pending.toLocaleString()}`;
                }

                // Annual Total
                const resAnnual = await fetch('/api/v1/reports/summary/annual', { headers: { 'Authorization': `Bearer ${token}` } });
                if (resAnnual.ok) {
                    const data = await resAnnual.json();
                    document.getElementById('annualTotal').textContent = `₦${data.total.toLocaleString()}`;
                }
            } catch (e) { console.error(e); }
        }

        async function triggerMonthly() {
            if (!confirm("Generate monthly dues for all active members (₦1,500 each)?")) return;
            const token = localStorage.getItem('access_token');
            try {
                const res = await fetch('/api/v1/contributions/generate-monthly', {
                    method: 'POST',
                    headers: { 'Authorization': `Bearer ${token}` }
                });
                if (res.ok) {
                    alert("Monthly dues generated successfully!");
                    fetchStats();
                }
            } catch (e) { console.error(e); }
        }

        async function sendReminders() {
            if (!confirm("Send contribution reminders to all members with pending payments?")) return;
            const token = localStorage.getItem('access_token');
            try {
                const res = await fetch('/api/v1/admin/trigger-reminders', {
                    method: 'POST',
                    headers: { 'Authorization': `Bearer ${token}` }
                });
                if (res.ok) {
                    const result = await res.json();
                    alert(result.message);
                }
            } catch (e) { console.error(e); }
        }

        fetchMembers();
        fetchStats();
    </script>
</body>

</html>